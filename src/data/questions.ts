// Thin shim — the real implementation lives in src/react-app/lib/questions.ts.
// This file exists so the existing `@/data/questions` imports keep resolving.
export * from "@/react-app/lib/questions";
