{
  "buildCommand": "node scripts/build-api.mjs && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!api/).+)", "destination": "/index.html" },
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
